import { SetLabelValueDirective } from './set-label-value.directive';

describe('SetLabelValueDirective', () => {
  it('should create an instance', () => {
    const directive = new SetLabelValueDirective();
    expect(directive).toBeTruthy();
  });
});
