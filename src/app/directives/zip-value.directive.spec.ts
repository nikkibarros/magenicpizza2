import { ZipValueDirective } from './zip-value.directive';

describe('ZipValueDirective', () => {
  it('should create an instance', () => {
    const directive = new ZipValueDirective();
    expect(directive).toBeTruthy();
  });
});
