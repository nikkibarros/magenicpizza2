import { ZipFormatDirective } from './zip-format.directive';

describe('ZipFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new ZipFormatDirective();
    expect(directive).toBeTruthy();
  });
});
